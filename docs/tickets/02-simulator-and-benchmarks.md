# Implement simulator and benchmark harness

Part of #1.

Implement seeded rendering/impairment fixtures for perspective, rotation, scale, blur, defocus, exposure, white-balance, glare, shadows, noise, quantization, rolling shutter, duplicate/drop frames and partial obstruction. Run the same decoder as the application, emit JSON diagnostics, compare robust/dense profiles, and report raw optical bitrate, encoded payload rate, verified original-file goodput and compression-adjusted goodput.

Done when benchmark results are reproducible from a seed and a small fixture suite gates protocol changes.


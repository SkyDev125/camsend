# Implement native Android endpoint

Part of #1.

Create a native Android app using CameraX/Camera2 ImageAnalysis with a keep-latest low-latency analyzer, native preview, send/receive modes, file picker/saving/sharing, optional AE/AF lock and display-brightness controls, and the same protocol golden vectors as the web core. Build a debug APK and document emulator limitations.


# Implement diagnostic export and recorded-fixture workflow

Part of #1.

Add a privacy-preserving diagnostic export shared by web and Android, plus import of recorded frame fixtures and optional explicitly enabled recordings. Exports must omit payload/file contents by default and include enough evidence to tune calibration, tracking, frame cadence, rejection causes and recovery overhead after physical tests.


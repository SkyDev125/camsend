# Camera/display synchronization and rolling shutter

Part of #1.

## Question

How should the protocol operate when sender refresh and camera capture are unsynchronized, frames are duplicated or dropped, and rolling shutter samples multiple display states? Decide whether Version 1 needs temporal preambles, rolling-shutter-aware decoding, frame IDs, exposure/flicker constraints, or adaptive cadence.
